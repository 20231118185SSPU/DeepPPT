三步完成环境配置：

**第一步**：访问 123everything.com，用邮箱注册账号。注册后赠送 0.2 美元额度，够生成几张图试试。

**第二步**：进入控制台 → 点击"API 密钥"→ 复制你的密钥。**这个密钥一定要保存好**，泄露了别人就能用你的额度。

**第三步**：打开小米工具箱里的 GPT-Image2 生成器 → 点击右上角设置 → 填入 URL（`vip.123everything.com/v1`）和你的 API Key。

注意检查余额——生成图片是消耗额度的。

（过渡：环境搭好了，我们来看标准工作流）